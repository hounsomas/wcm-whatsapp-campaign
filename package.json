{
  "name": "wcm-whatsapp-campaign-manager",
  "version": "1.0.0",
  "description": "Plateforme de gestion de campagnes WhatsApp avec programmation et rapports",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "o2switch-build": "npm run install-client && npm run build && echo 'Build completed for O2switch'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.2",
    "uuid": "^9.0.0",
    "dotenv": "^16.3.1",
    "axios": "^1.5.0",
    "sqlite3": "^5.1.6",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-rate-limit": "^6.10.0",
    "helmet": "^7.0.0",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "whatsapp",
    "campaign",
    "messaging",
    "automation"
  ],
  "author": "WCM Team",
  "license": "MIT"
} 